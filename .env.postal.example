# Postal Configuration
# Copy this file to .env.postal and update the values

# Your mail domain (e.g., mail.example.com)
POSTAL_DOMAIN=postal.example.com
POSTAL_WEB_HOST=postal.example.com

# MySQL root password for Postal's database
MYSQL_ROOT_PASSWORD=change_this_secure_password

# Secret key for Rails (generate with: openssl rand -hex 64)
POSTAL_SECRET_KEY=generate_a_random_64_character_string_here

# SMTP credentials for Pulsar to use
# After setting up Postal, create an organization and SMTP credentials
# Then update Pulsar's .env with:
#   SMTP_HOST=postal-smtp
#   SMTP_PORT=25
#   SMTP_USERNAME=your_postal_smtp_username
#   SMTP_PASSWORD=your_postal_smtp_password
#   SMTP_FROM=noreply@yourdomain.com
#   SMTP_USE_TLS=false
